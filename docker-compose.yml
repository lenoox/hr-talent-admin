services:
  www:
    build:
      dockerfile: Dockerfile
      context: .
      target: development
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run start
    ports:
      - '4202:4202'
    networks:
      - hr-talent-network
networks:
  hr-talent-network:
